import { Route } from '@angular/router';
import { ContactFormComponent } from './contact-form/contact-form.component';

export const contactFormRoutes: Route[] = [
  { path: '', component: ContactFormComponent },
];
