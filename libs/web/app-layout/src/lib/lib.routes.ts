import { Route } from '@angular/router';
import { AppLayoutComponent } from './app-layout/app-layout.component';

export const appLayoutRoutes: Route[] = [
  { path: '', component: AppLayoutComponent },
];
